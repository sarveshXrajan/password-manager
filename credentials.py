# Change the values of host, username, and password to your own SQL credentials.
# Then run the code in app.py


import mysql.connector as sql

# SQL CREDENTIALS TO RUN CODE
host =
username =
password =

conn = sql.connect(host=host, user=username, password=password)
